{
    "name": "vscodeilviewer",
    "displayName": "VsCodeIlViewer",
    "description": "IL Viewer for Visual Studio Code",
    "version": "0.0.1",
    "publisher": "JosephWoodward",
    "engines": {
        "vscode": "^1.5.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.viewIl"
    ],
    "main": "./out/src/extension",
    "contributes": {
        "configuration": {
            "properties": {
                
            }
        },
        "commands": [{
            "command": "extension.viewIl",
            "title": "View IL"
        },
        {
            "command": "editor.printReferences",
            "title": "View IL"
        }],
        "keybindings": [{
            "command": "extension.viewIl",
            "key": "shift+ctrl+v",
            "mac": "shift+cmd+v",
            "when": "editorTextFocus && editorLangId == csharp"
        }]
    },
    "scripts": {
        "vscode:prepublish": "tsc -p ./",
        "compile": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/vscode/bin/install",
        "test": "node ./node_modules/vscode/bin/test"
    },
    "devDependencies": {
        "typescript": "^2.0.3",
        "vscode": "^1.0.0",
        "mocha": "^2.3.3",
        "@types/node": "^6.0.40",
        "@types/mocha": "^2.2.32"
    }
}